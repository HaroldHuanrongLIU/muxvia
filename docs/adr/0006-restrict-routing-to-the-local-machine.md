# Restrict routing to the local machine

The first release will expose model routes only to programs on the same machine and will not offer unauthenticated LAN listening. This matches the single-Operator scope and avoids turning locally stored API and subscription credentials into a network-accessible relay.
