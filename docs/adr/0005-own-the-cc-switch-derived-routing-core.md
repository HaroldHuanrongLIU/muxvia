# Own the CC-Switch-derived routing core

Muxvia will not require CC-Switch to be installed or running. It will extract and maintain the relevant MIT-licensed Rust routing behavior inside its own headless Routing Service, preserving required notices and compatibility evidence while excluding the Tauri GUI lifecycle and other unsupported client surfaces.
