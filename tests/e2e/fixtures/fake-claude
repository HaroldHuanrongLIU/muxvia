#!/bin/sh
set -eu
case "${1-}" in
  --version)
    printf '%s\n' '2.1.37 (Claude Code)'
    ;;
  --help)
    printf '%s\n' 'Usage: claude [OPTIONS]'
    ;;
  *)
    printf '%s\n' 'unexpected fake Claude invocation' >&2
    exit 64
    ;;
esac
