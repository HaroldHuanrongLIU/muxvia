#!/bin/sh
set -eu
case "${1-}" in
  --version)
    printf '%s\n' 'codex-cli 0.106.0'
    ;;
  --help)
    printf '%s\n' 'Usage: codex [OPTIONS]' '--config <key=value>'
    ;;
  *)
    printf '%s\n' 'unexpected fake Codex invocation' >&2
    exit 64
    ;;
esac
